services:
  - type: web
    name: esgi-2i1
    runtime: node
    region: frankfurt
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: DB_CONNECTION_STRING
        valueFromDatabase:
          property: connectionString
          database: esgi
databases:
  - name: esgi
    region: frankfurt
